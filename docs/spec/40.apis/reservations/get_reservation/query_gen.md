<!-- This file is generated by uv run python -m tools.generate_query_specs. Do not edit manually. -->

# get_reservation query

## 001_select_reservations.sql

### SQL種別

`SELECT`

### SQLの概要

予約詳細を返すため、指定した予約を取得する。

### 利用するテーブル

- `reservations`

### 引数

| DDLテーブル | DDL項目 | SQL項目 | 日本語名 | 型 | nullable |
| --- | --- | --- | --- | --- | --- |
| <code>reservations</code> | <code>reservation_id</code> | <code>reservation_id</code> | 予約ID。 | <code>VARCHAR(36)</code> | no |

### 戻り値

| DDLテーブル | DDL項目 | SQL項目 | 日本語名 | 型 | nullable |
| --- | --- | --- | --- | --- | --- |
| <code>reservations</code> | <code>reservation_id</code> | <code>reservation_id</code> | 予約ID。 | <code>VARCHAR(36)</code> | no |
| <code>reservations</code> | <code>resource_id</code> | <code>resource_id</code> | 予約対象の資源ID。 | <code>VARCHAR(36)</code> | no |
| <code>reservations</code> | <code>owner_principal_id</code> | <code>owner_principal_id</code> | 予約者の認証subject。 | <code>VARCHAR(200)</code> | no |
| <code>reservations</code> | <code>start_at</code> | <code>start_at</code> | 予約開始日時。UTCで保存する。 | <code>TIMESTAMPTZ</code> | no |
| <code>reservations</code> | <code>end_at</code> | <code>end_at</code> | 予約終了日時。UTCで保存し、この時刻を含まない。 | <code>TIMESTAMPTZ</code> | no |
| <code>reservations</code> | <code>purpose</code> | <code>purpose</code> | 予約目的。1〜200文字。本人と管理者だけに返す。 | <code>VARCHAR(200)</code> | no |
| <code>reservations</code> | <code>status</code> | <code>status</code> | 予約状態。confirmedまたはcancelled。 | <code>VARCHAR(20)</code> | no |
| <code>reservations</code> | <code>row_version</code> | <code>row_version</code> | 楽観ロック用の行バージョン。 | <code>INT</code> | no |

### 条件

- `WHERE`
  - `reservations.reservation_id = $reservation_id`


## 002_select_reservation_events.sql

### SQL種別

`SELECT`

### SQLの概要

予約詳細の履歴を返すため、作成と取消の履歴を発生順に取得する。

### 利用するテーブル

- `reservation_events`

### 引数

| DDLテーブル | DDL項目 | SQL項目 | 日本語名 | 型 | nullable |
| --- | --- | --- | --- | --- | --- |
| <code>reservation_events</code> | <code>reservation_id</code> | <code>reservation_id</code> | 対象の予約ID。 | <code>VARCHAR(36)</code> | no |

### 戻り値

| DDLテーブル | DDL項目 | SQL項目 | 日本語名 | 型 | nullable |
| --- | --- | --- | --- | --- | --- |
| <code>reservation_events</code> | <code>event_id</code> | <code>event_id</code> | 履歴ID。 | <code>VARCHAR(36)</code> | no |
| <code>reservation_events</code> | <code>reservation_id</code> | <code>reservation_id</code> | 対象の予約ID。 | <code>VARCHAR(36)</code> | no |
| <code>reservation_events</code> | <code>actor_principal_id</code> | <code>actor_principal_id</code> | 操作した利用者の認証subject。 | <code>VARCHAR(200)</code> | no |
| <code>reservation_events</code> | <code>action</code> | <code>action</code> | 操作種別。createdまたはcancelled。 | <code>VARCHAR(20)</code> | no |
| <code>reservation_events</code> | <code>occurred_at</code> | <code>occurred_at</code> | 操作日時。 | <code>TIMESTAMPTZ</code> | no |

### 条件

- `WHERE`
  - `reservation_events.reservation_id = $reservation_id`
