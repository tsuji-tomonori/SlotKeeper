<!-- This file is generated by uv run python -m tools.generate_query_specs. Do not edit manually. -->

# update_resource query

## 001_update_resources_control_version.sql

### SQL種別

`UPDATE`

### SQLの概要

同一資源の予約作成・取消と競合させるため、資源の内部制御版を進めて現在値を取得する。

### 利用するテーブル

- `resources`

### 引数

| DDLテーブル | DDL項目 | SQL項目 | 日本語名 | 型 | nullable |
| --- | --- | --- | --- | --- | --- |
| <code>resources</code> | <code>resource_id</code> | <code>resource_id</code> | 資源ID。 | <code>VARCHAR(36)</code> | no |

### 戻り値

| DDLテーブル | DDL項目 | SQL項目 | 日本語名 | 型 | nullable |
| --- | --- | --- | --- | --- | --- |
| <code>resources</code> | <code>resource_id</code> | <code>resource_id</code> | 資源ID。 | <code>VARCHAR(36)</code> | no |
| <code>resources</code> | <code>active</code> | <code>active</code> | 新規予約を受け付けるかどうか。 | <code>BOOLEAN</code> | no |
| <code>resources</code> | <code>row_version</code> | <code>row_version</code> | 利用者に公開する楽観ロック用の行バージョン。 | <code>INT</code> | no |

### 条件

- `WHERE`
  - `resource_id = $resource_id`


## 002_select_reservations.sql

### SQL種別

`SELECT`

### SQLの概要

資源の無効化可否を判定するため、開始前の確定予約の件数を取得する。

### 利用するテーブル

- `reservations`

### 引数

| DDLテーブル | DDL項目 | SQL項目 | 日本語名 | 型 | nullable |
| --- | --- | --- | --- | --- | --- |
| <code>reservations</code> | <code>resource_id</code> | <code>resource_id</code> | 予約対象の資源ID。 | <code>VARCHAR(36)</code> | no |
| <code>reservations</code> | <code>start_at</code> | <code>now</code> | 予約開始日時。UTCで保存する。 | <code>TIMESTAMPTZ</code> | no |

### 戻り値

| DDLテーブル | DDL項目 | SQL項目 | 日本語名 | 型 | nullable |
| --- | --- | --- | --- | --- | --- |
| <code>-</code> | <code>future_reservation_count</code> | <code>future_reservation_count</code> | - | <code>int</code> | no |

### 条件

- `WHERE`
  - `reservations.resource_id = $resource_id`
  - `AND reservations.status = 'confirmed'`
  - `AND reservations.start_at > $now`


## 003_update_resources.sql

### SQL種別

`UPDATE`

### SQLの概要

資源の名前・説明・種別・有効状態を更新し、公開版を1つ進める。

### 利用するテーブル

- `resources`

### 引数

| DDLテーブル | DDL項目 | SQL項目 | 日本語名 | 型 | nullable |
| --- | --- | --- | --- | --- | --- |
| <code>resources</code> | <code>name</code> | <code>name</code> | 資源名。空白除去後1〜100文字。 | <code>VARCHAR(100)</code> | no |
| <code>resources</code> | <code>description</code> | <code>description</code> | 資源の説明。0〜1,000文字。 | <code>VARCHAR(1000)</code> | no |
| <code>resources</code> | <code>kind</code> | <code>kind</code> | 資源種別。roomまたはequipment。 | <code>VARCHAR(20)</code> | no |
| <code>resources</code> | <code>active</code> | <code>active</code> | 新規予約を受け付けるかどうか。 | <code>BOOLEAN</code> | no |
| <code>resources</code> | <code>resource_id</code> | <code>resource_id</code> | 資源ID。 | <code>VARCHAR(36)</code> | no |

### 戻り値

| DDLテーブル | DDL項目 | SQL項目 | 日本語名 | 型 | nullable |
| --- | --- | --- | --- | --- | --- |
| <code>resources</code> | <code>resource_id</code> | <code>resource_id</code> | 資源ID。 | <code>VARCHAR(36)</code> | no |
| <code>resources</code> | <code>name</code> | <code>name</code> | 資源名。空白除去後1〜100文字。 | <code>VARCHAR(100)</code> | no |
| <code>resources</code> | <code>description</code> | <code>description</code> | 資源の説明。0〜1,000文字。 | <code>VARCHAR(1000)</code> | no |
| <code>resources</code> | <code>kind</code> | <code>kind</code> | 資源種別。roomまたはequipment。 | <code>VARCHAR(20)</code> | no |
| <code>resources</code> | <code>active</code> | <code>active</code> | 新規予約を受け付けるかどうか。 | <code>BOOLEAN</code> | no |
| <code>resources</code> | <code>row_version</code> | <code>row_version</code> | 利用者に公開する楽観ロック用の行バージョン。 | <code>INT</code> | no |

### 条件

- `WHERE`
  - `resource_id = $resource_id`
