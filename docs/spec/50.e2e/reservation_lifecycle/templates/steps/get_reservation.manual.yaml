schema_version: 1
template:
  id: get_reservation
  operation_id: getReservation
  method: GET
  path: /reservations/${reservationId}
request:
  headers:
    Authorization: Bearer ${tokens.member_token}
